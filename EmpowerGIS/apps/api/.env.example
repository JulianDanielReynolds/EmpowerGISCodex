NODE_ENV=development
PORT=4000

DATABASE_URL=postgresql://empowergis:empowergis_dev_password@localhost:5432/empowergis

JWT_ACCESS_SECRET=replace-with-a-long-random-secret
JWT_ACCESS_TTL_MINUTES=15
REFRESH_TOKEN_TTL_DAYS=14
BCRYPT_ROUNDS=12

# Comma-separated
CORS_ORIGINS=http://localhost:5173,http://localhost:4173

# Optional. In development defaults to http://localhost:<PORT>/tiles
TILE_BASE_URL=http://localhost:4000/tiles

# Optional local tile directory for /tiles route.
# If omitted, the API auto-detects /data-pipeline/output/tiles in this repository.
# TILES_DIR=/absolute/path/to/EmpowerGIS/data-pipeline/output/tiles
